# CNIPA bibliographic search (skills/patent-search). Same playwright as root requirements.txt.
# System Chrome/Edge is enough; no need for `playwright install chromium`.
# Probe: python tools/browser.py --probe
playwright>=1.40.0
