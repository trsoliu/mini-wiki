# 申请文件合同

| 文件 | 用途 |
|------|------|
| `application_plan.schema.yaml` | 内部策略备忘（含从属树、点名图） |
| `numeral_register.schema.yaml` | 件号 / 模块标记登记 |
| `invention_figures.schema.yaml` | 发明框图、流程图、点名 `source_image` |

词库（非合同）：`../promo_terms.yaml`，权要与摘要宣传语检查共用。

交底侧 schema 仍在交底包，本包只读。
